@model IEnumerable<Minimarket_Raphi.Models.Subfamilia_Producto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Agregar Nuevo", "Create")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_HojaRegistro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subfamilia)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_HojaRegistro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subfamilia)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Subfamilia }) |
                @Html.ActionLink("Details", "Details", new { id = item.Subfamilia }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Subfamilia })
            </td>
        </tr>
    }
</table>